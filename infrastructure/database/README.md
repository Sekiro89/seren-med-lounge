# infrastructure/database/

Reserved for database operational scripts (backup/restore, seed data for
staging, one-off maintenance scripts) that don't belong inside
`apps/api/prisma/` (which is schema + migrations only).
