# components/

Shared, reusable presentational components local to `patient-web` (not reusable
across apps — those belong in `@serenemed/ui`). No business logic here: fetch
data in `features/*`, pass it down as props.
