# features/

Cross-workspace capabilities that more than one role touches (e.g. the
unified patient record viewer, the timeline component, the queue board).
Role-specific screens live under `../workspaces/<role>/` instead and compose
these features.

Not scaffolded yet: added as backend modules ship.
