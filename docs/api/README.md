# docs/api/

The authoritative API reference is generated Swagger/OpenAPI, served at
`/docs` when `apps/api` is running (see `apps/api/src/main.ts`). This
folder is reserved for hand-written API guides (auth flows, pagination
conventions, error-shape reference) once there's enough surface area to
warrant them beyond what Swagger already documents.
