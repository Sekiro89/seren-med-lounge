{
  "name": "@serenemed/permissions",
  "version": "0.1.0",
  "private": true,
  "description": "Central role/permission matrix. Backend is the source of truth for authorization; frontends use this package only to decide what to render, never to enforce access.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --max-warnings=0"
  },
  "dependencies": {
    "@serenemed/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "eslint": "^9.18.0"
  }
}
